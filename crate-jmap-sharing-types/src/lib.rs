// jmap-sharing-types — JMAP Sharing data types (RFC 9670).
// Defines Principal and ShareNotification — the cross-cutting sharing
// primitives used by all shareable JMAP data types (mail, calendars,
// contacts, filenode).
// See PLAN.md for implementation plan.
