{
  "assignee": "Mark Atwood",
  "close_reason": "Closed",
  "closed_at": "2026-05-14T21:29:48Z",
  "compaction_level": 0,
  "content_hash": "bc82b1b50ecb0b9b79ecd8092540153455a1ce3e09a5d5a71c7cea5e096e02df",
  "created_at": "2026-05-14T19:51:03Z",
  "created_by": "Mark Atwood",
  "description": "src/filenode.rs:489-515: cycle check uses 'patch_val.get(\"parentId\")' to find the new parent. This works for the common flat patch shape { 'parentId': 'foo' } but RFC 8620 \u00a75.3 PatchObject permits JSON Pointer paths as keys. The shape '{ \"parentId/0\": ... }' is not legal for the parentId field (Id is a scalar), but the introspection is hand-rolled rather than going through the typed PatchObject.\n\nThe bigger issue: the patch is later parsed as PatchObject at filenode.rs:520. If PatchObject's deserializer normalizes pointer-form keys (e.g., 'parentId' is accepted as the only legal form), then the introspection is fine. If it does not, the introspection here is divergent from the patch the backend actually receives.\n\nA correctness consequence: the cycle check at line 489 reads the raw wire Value before patch-typing. The handler then deserializes a different patch via PatchObject's potentially looser/different semantics. Mismatch surface: a patch the cycle-check sees as 'not touching parentId' could end up changing parentId after the typed deserialization, bypassing the cycle guard.\n\nThe fix is to do the cycle check on the typed PatchObject, not on the raw wire Value. PatchObject is the contract that flows to backend.update_object; the handler's cycle check must agree with that contract.\n\nTrigger: depends on PatchObject's exact deserialization behavior \u2014 but the architectural problem (read raw Value for guard, then re-deserialize as PatchObject) is the bug regardless of whether the current jmap-types PatchObject impl makes it exploitable today.\n\nRequired: parse patch_val into PatchObject first, then read the parentId field from PatchObject. If PatchObject does not expose typed field access, file a bd issue against jmap-types to add it.",
  "ephemeral": 0,
  "id": "JMAP-510h.13",
  "is_blocked": 0,
  "is_template": 0,
  "issue_type": "bug",
  "metadata": {},
  "no_history": 0,
  "owner": "mark@reviewcommit.com",
  "pinned": 0,
  "priority": 1,
  "status": "closed",
  "timeout_ns": 0,
  "title": "cycle check uses wire JSON Value 'parentId' lookup, ignores JSON-pointer patch keys",
  "updated_at": "2026-05-14T21:29:48Z",
  "dependencies": [
    {
      "created_at": "2026-05-14T12:51:02Z",
      "created_by": "Mark Atwood",
      "depends_on_issue_id": "JMAP-510h",
      "id": "e1d3b5e8-097e-5478-995a-54b97ed3953c",
      "metadata": {},
      "type": "parent-child"
    }
  ]
}
