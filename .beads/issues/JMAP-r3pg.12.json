{
  "assignee": "mark@reviewcommit.com",
  "close_reason": "Extracted finalize_set_response<B,O> helper in src/helpers.rs covering the state recompute + RFC 8620 \u00a75.3 envelope construction. Four handlers (Calendar, CalendarEvent, CalendarEventNotification, ParticipantIdentity) now end with a one-line forwarding call. The further SetResponseBuilder<O> bundle (renaming ~150 accumulator vars) is a tractable follow-on; annotated #[allow(clippy::too_many_arguments)] with a comment pointing to it. Workspace tests green (96 result entries, 0 failed; calendars-server 66/66 passing). Commit fca0c74.",
  "closed_at": "2026-05-09T03:02:03Z",
  "compaction_level": 0,
  "content_hash": "cfec9b747557c7d467041673fbc095825dc8b9136832c22c39a17b9b19e75292",
  "created_at": "2026-05-08T03:32:51Z",
  "created_by": "Mark Atwood",
  "description": "calendar.rs (handle_calendar_set), event.rs (handle_calendar_event_set), event_notification.rs (handle_calendar_event_notification_set), participant_identity.rs (handle_participant_identity_set) all end with the identical 9-line response-construction block:\n\n    Ok((\n        json!({\n            \"accountId\": account_id.as_ref(),\n            \"oldState\": old_state.as_ref(),\n            \"newState\": new_state.as_ref(),\n            \"created\":      if created.is_empty()        { Value::Null } else { Value::Object(created) },\n            \"updated\":      if updated.is_empty()        { Value::Null } else { Value::Object(updated) },\n            \"destroyed\":    if destroyed_list.is_empty() { Value::Null } else { Value::Array(destroyed_list) },\n            \"notCreated\":   if not_created.is_empty()    { Value::Null } else { Value::Object(not_created) },\n            \"notUpdated\":   if not_updated.is_empty()    { Value::Null } else { Value::Object(not_updated) },\n            \"notDestroyed\": if not_destroyed.is_empty()  { Value::Null } else { Value::Object(not_destroyed) },\n        }),\n        vec![],\n    ))\n\nA second identical block is duplicated for state computation:\n\n    let new_state = if mutated {\n        backend.get_state::<O>(&account_id).await.map_err(|e| JmapError::server_fail(e.to_string()))?\n    } else {\n        old_state.clone()\n    };\n\nAnd the per-id error-mapping pattern (BackendSetError::SetError -> set_error_value, BackendSetError::Other -> serverFail json) appears five times in event.rs alone.\n\nIdiom recommendation: extract a SetResponseBuilder<O> in src/helpers.rs that owns the six maps and emits the final json envelope. Or at minimum extract two helpers:\n- fn set_envelope(account_id, old, new, created, updated, destroyed, not_created, not_updated, not_destroyed) -> Value\n- fn map_set_error(e: BackendSetError<E>, err: &mut serde_json::Map<String, Value>, key: String)\n\nThe locked jmap-mail-server has the same four duplicated blocks pattern (in mailbox.rs, email.rs, identity.rs, etc.) so this finding is parallel to a likely future refactor across both crates. Worth coordinating.\n\nSeverity: P2 idiom \u2014 code works, just verbose. ~150 LoC could go away. Reduces surface for state-shape divergence between the four /set handlers (e.g. one of them forgetting to null an empty map).",
  "ephemeral": 0,
  "id": "JMAP-r3pg.12",
  "is_blocked": 0,
  "is_template": 0,
  "issue_type": "task",
  "metadata": {},
  "no_history": 0,
  "owner": "mark@reviewcommit.com",
  "pinned": 0,
  "priority": 2,
  "status": "closed",
  "timeout_ns": 0,
  "title": "Boilerplate /set output construction repeats across 4 handlers \u2014 extract a helper",
  "updated_at": "2026-05-09T03:02:03Z",
  "dependencies": [
    {
      "created_at": "2026-05-07T20:32:51Z",
      "created_by": "Mark Atwood",
      "depends_on_issue_id": "JMAP-r3pg",
      "id": "08fa9012-4338-5b6d-9e35-65733b4b895f",
      "metadata": {},
      "type": "parent-child"
    }
  ]
}
