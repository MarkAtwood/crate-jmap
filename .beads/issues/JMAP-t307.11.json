{
  "assignee": "Mark Atwood",
  "close_reason": "Landed in 8d12c8e. The four push_str(&format!(...)) sites that the bead named (originally in benches/mime_pipeline.rs; relocated by JMAP-t307.1 to tests/common/mod.rs) are now writeln!(s, ...).expect('writing to String') using std::fmt::Write. One intermediate String allocation eliminated per call.",
  "closed_at": "2026-05-15T06:35:47Z",
  "compaction_level": 0,
  "content_hash": "926e137acb4ae08ee0a7966797abe1c4e209466da7f3fed3f9a470f567fd5d18",
  "created_at": "2026-05-15T06:06:27Z",
  "created_by": "Mark Atwood",
  "description": "What the code does: crate-jmap-mime/benches/mime_pipeline.rs builds fixture byte strings by chaining `s.push_str(&format!(\"...\", x))` four times \u2014 at lines 88, 143, 167, 185 (clippy pedantic `format_push_string` flags all four).\n\nWhat idiomatic Rust would do: bring `std::fmt::Write` into scope and use `write!(s, \"...\", x).expect(\"writing to String never fails\")` or `writeln!(...)`. This is the standard pattern for incrementally building a `String` with formatted content; `push_str(&format!(...))` allocates an intermediate `String` that is immediately copied and dropped.\n\nWhy it matters: this is a Rust practitioner instinct, not a style preference \u2014 clippy's pedantic `format_push_string` exists because (a) the extra allocation has measurable cost in hot code, (b) the `write!` form scales to multiple format args on one line without nesting, and (c) it composes with `?` in functions that return `Result`. In bench-fixture setup the perf delta is negligible (fixtures build once outside the timed `b.iter`), but the pattern is read as the canonical 'how to build a `String`' example by anyone learning from this code. Four instances is enough to set a local style; fixing them aligns with the rest of the bench file which uses bare `push_str` everywhere else.\n\nConcrete sites:\n- crate-jmap-mime/benches/mime_pipeline.rs:88 \u2014 `s.push_str(&format!(\"Content-ID: <part-{}@example.com>\\\\r\\\\n\", idx));`\n- crate-jmap-mime/benches/mime_pipeline.rs:143 \u2014 `s.push_str(&format!(\"Content-Type: multipart/mixed; boundary=\\\"{}\\\"\\\\r\\\\n\", boundaries[0]));`\n- crate-jmap-mime/benches/mime_pipeline.rs:167 \u2014 `out.push_str(&format!(\"Content-ID: <L{}-leaf-{}@example.com>\\\\r\\\\n\", level + 1, leaf_idx));`\n- crate-jmap-mime/benches/mime_pipeline.rs:185 \u2014 `out.push_str(&format!(\"Content-Type: multipart/mixed; boundary=\\\"{}\\\"\\\\r\\\\n\", inner));`\n\nSuggested fix:\n```rust\nuse std::fmt::Write as _;\n// ...\nwrite!(s, \"Content-ID: <part-{idx}@example.com>\\\\r\\\\n\").expect(\"writing to String\");\n```\n\nOut of scope: src/lib.rs is unchanged in scope; the adapter code does not use this pattern. The fix is entirely in benches/mime_pipeline.rs.",
  "ephemeral": 0,
  "id": "JMAP-t307.11",
  "is_blocked": 0,
  "is_template": 0,
  "issue_type": "chore",
  "metadata": {},
  "no_history": 0,
  "owner": "mark@reviewcommit.com",
  "pinned": 0,
  "priority": 2,
  "status": "closed",
  "timeout_ns": 0,
  "title": "Bench builders use `s.push_str(&format!(...))` \u2014 should be `write!` via fmt::Write",
  "updated_at": "2026-05-15T06:35:47Z",
  "dependencies": [
    {
      "created_at": "2026-05-14T23:06:27Z",
      "created_by": "Mark Atwood",
      "depends_on_issue_id": "JMAP-t307",
      "id": "995aed9b-24cc-5069-864a-ef7589fffd56",
      "metadata": {},
      "type": "parent-child"
    }
  ]
}
