{
  "assignee": "Mark Atwood",
  "close_reason": "Typed CalendarEventGetArgs.recurrence_overrides_{before,after} as Option<UTCDate>. Handler validates via UTCDate::new_validated and rejects malformed input as invalidArguments at the handler boundary, matching the principal.rs:51-59 pattern. Two regression tests added covering both fields. PLAN.md and rustdoc updated. fmt/clippy/test/doc all green.",
  "closed_at": "2026-05-15T18:28:00Z",
  "compaction_level": 0,
  "content_hash": "819eb7dd88f1cb40b30ee02efd9f5c2b8f566b64dabe86ec7277ed01b5fa96f4",
  "created_at": "2026-05-15T18:07:15Z",
  "created_by": "Mark Atwood",
  "description": "crate-jmap-calendars-server/src/backend.rs:667 and :671 expose `pub recurrence_overrides_before: Option<String>` and `pub recurrence_overrides_after: Option<String>`. The rustdoc on each says the value is a UTCDateTime that the backend must compare against recurrence-id-translated-to-UTC values.\n\n`jmap_types::UTCDate` already exists in the dep graph and IS used elsewhere in the same trait:\n- line 351 (compute_utc_times return)\n- lines 556-557 (get_availability `utc_start` / `utc_end`)\n- The Principal/getAvailability handler at principal.rs:51-59 even uses `UTCDate::new_validated` to reject malformed values BEFORE the backend sees them.\n\nYet the same trait routes the structurally-identical recurrence_overrides_before/after through `Option<String>`. Every backend implementor is forced to re-parse on every CalendarEvent/get call, and a malformed UTCDateTime reaches the backend rather than being rejected as `invalidArguments` at the handler boundary (see event.rs:69-77 which just calls `.as_str().map(to_owned)`).\n\nWhat callers cannot rely on:\n- That the value reaching the backend is a syntactically-valid UTCDateTime.\n- That two backend impls agree on what counts as valid.\n- That a malformed string is the handler's fault to reject (no signal in the type).\n\nThe fix is to change the field type to `Option<UTCDate>` and have the handler validate via `UTCDate::new_validated` exactly as `principal.rs:51-59` does for `utcStart`/`utcEnd`. Non-breaking now (pre-1.0); SemVer-breaking later. Should land before the args struct stabilises.",
  "ephemeral": 0,
  "id": "JMAP-ic0j.55",
  "is_blocked": 0,
  "is_template": 0,
  "issue_type": "task",
  "metadata": {},
  "no_history": 0,
  "owner": "mark@reviewcommit.com",
  "pinned": 0,
  "priority": 2,
  "status": "closed",
  "timeout_ns": 0,
  "title": "CalendarEventGetArgs.recurrence_overrides_before/after: Option<String> instead of Option<UTCDate>",
  "updated_at": "2026-05-15T18:28:00Z",
  "dependencies": [
    {
      "created_at": "2026-05-15T11:07:14Z",
      "created_by": "Mark Atwood",
      "depends_on_issue_id": "JMAP-ic0j",
      "id": "0b929730-0ee7-5d5f-a97d-12486d7e310f",
      "metadata": {},
      "type": "parent-child"
    }
  ]
}
